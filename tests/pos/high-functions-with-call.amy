object F

    fn appendHello(s: String) : String = {
        "Hello" ++ " " ++ s
    }

    fn test(s: String, f : (String) => Unit): Unit = {
        f(s)
    }

    val t : String = test("World", Std::printString);
    Std.printString(t)

end F